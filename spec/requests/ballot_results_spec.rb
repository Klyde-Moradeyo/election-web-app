require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/ballot_results", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # BallotResult. As you add validations to BallotResult, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      BallotResult.create! valid_attributes
      get ballot_results_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      ballot_result = BallotResult.create! valid_attributes
      get ballot_result_url(ballot_result)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_ballot_result_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      ballot_result = BallotResult.create! valid_attributes
      get edit_ballot_result_url(ballot_result)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new BallotResult" do
        expect {
          post ballot_results_url, params: { ballot_result: valid_attributes }
        }.to change(BallotResult, :count).by(1)
      end

      it "redirects to the created ballot_result" do
        post ballot_results_url, params: { ballot_result: valid_attributes }
        expect(response).to redirect_to(ballot_result_url(BallotResult.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new BallotResult" do
        expect {
          post ballot_results_url, params: { ballot_result: invalid_attributes }
        }.to change(BallotResult, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post ballot_results_url, params: { ballot_result: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested ballot_result" do
        ballot_result = BallotResult.create! valid_attributes
        patch ballot_result_url(ballot_result), params: { ballot_result: new_attributes }
        ballot_result.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the ballot_result" do
        ballot_result = BallotResult.create! valid_attributes
        patch ballot_result_url(ballot_result), params: { ballot_result: new_attributes }
        ballot_result.reload
        expect(response).to redirect_to(ballot_result_url(ballot_result))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        ballot_result = BallotResult.create! valid_attributes
        patch ballot_result_url(ballot_result), params: { ballot_result: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested ballot_result" do
      ballot_result = BallotResult.create! valid_attributes
      expect {
        delete ballot_result_url(ballot_result)
      }.to change(BallotResult, :count).by(-1)
    end

    it "redirects to the ballot_results list" do
      ballot_result = BallotResult.create! valid_attributes
      delete ballot_result_url(ballot_result)
      expect(response).to redirect_to(ballot_results_url)
    end
  end
end
